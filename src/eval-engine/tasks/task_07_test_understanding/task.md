# Task 07: Filter and Double

## What the code should do

`filter_and_double(items, threshold)` returns a list of items that are **greater than or equal to** the threshold, each multiplied by 2. Order is preserved.

- `filter_and_double([1, 5, 3, 5, 2], 5)` → `[10, 10]`
- `filter_and_double([4, 5, 6], 4)` → `[8, 10, 12]`
- `filter_and_double([1, 2, 3], 10)` → `[]`

## What's wrong

The tests are failing. Fix the code so all tests pass.

## File to fix

`starter/module.py`

## Category

Correct Code / Wrong Test Understanding (read the docstring and tests carefully before changing anything)
