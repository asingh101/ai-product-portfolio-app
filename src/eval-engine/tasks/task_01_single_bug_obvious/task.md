# Task 01: Sum List

## What the code should do

`sum_list(items)` takes a list of numbers and returns their total sum.

- `sum_list([1, 2, 3])` → `6`
- `sum_list([])` → `0`
- `sum_list([-1, 1])` → `0`

## What's wrong

The tests are failing. Fix the code so all tests pass.

## File to fix

`starter/module.py`

## Category

Single Bug — Obvious Error
