# Task 10: Capitalize Words

## What the code should do

`capitalize_words(text)` capitalizes the first letter of each word (space-separated).
Multiple spaces between words are preserved as-is.

- `capitalize_words("hello world")` → `"Hello World"`
- `capitalize_words("foo  bar")` → `"Foo  Bar"`
- `capitalize_words("hello world!")` → `"Hello World!"`

## What's wrong

The tests are failing. Fix the code so all tests pass.

## File to fix

`starter/module.py`

## Note

The loop style may look unusual — that is intentional and not the source of the bug.

## Category

Red Herring (the loop structure looks like it could be cleaned up, but that is not what is causing the failure)
