# Task 02: Get Maximum

## What the code should do

`get_max(numbers)` takes a list of numbers and returns the largest one.
Returns `None` if the list is empty.

- `get_max([3, 1, 4, 1, 5])` → `5`
- `get_max([])` → `None`
- `get_max([-5, -2, -8])` → `-2`

## What's wrong

The tests are failing. Fix the code so all tests pass.

## File to fix

`starter/module.py`

## Category

Single Bug — Obvious Error
