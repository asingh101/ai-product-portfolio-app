# Task 06: Find Second Largest

## What the code should do

`find_second_largest(numbers)` returns the second largest unique value in the list.
Returns `None` if there are fewer than 2 unique values.

- `find_second_largest([3, 1, 4, 1, 5])` → `4`
- `find_second_largest([5, 5, 5])` → `None`
- `find_second_largest([10, 20])` → `10`

## What's wrong

The tests are failing. Fix the code so all tests pass.

## File to fix

`starter/module.py`

## Category

Two Bugs — Sequential (fix the first failure, a second will appear)
