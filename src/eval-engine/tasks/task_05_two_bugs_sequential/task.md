# Task 05: Calculate Grade

## What the code should do

`calculate_grade(scores)` takes a list of test scores and returns the letter grade for their average.

Grade scale: A (>=90), B (>=80), C (>=70), D (>=60), F (<60).
Returns `'F'` for an empty list.

- `calculate_grade([95, 90, 100])` → `'A'`
- `calculate_grade([60])` → `'D'`
- `calculate_grade([])` → `'F'`

## What's wrong

The tests are failing. Fix the code so all tests pass.

## File to fix

`starter/module.py`

## Category

Two Bugs — Sequential (fix the first failure, a second will appear)
